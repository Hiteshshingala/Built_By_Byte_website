export const metadata = {
  title: 'Get a Project Estimate',
  description:
    'Ready to build? Book a free 20-min intro call with BuiltByByte. No pitch deck required — just tell us what you\'re building.',
};

export default function ContactLayout({ children }) {
  return children;
}
